{
  "name": "smart-news-search",
  "version": "1.0.0",
  "description": "An intelligent web tool for real-time searching of national and international news.",
  "scripts": {
    "dev": "esbuild index.tsx --bundle --outfile=index.js --format=esm --jsx=automatic --servedir=. --serve=3000",
    "build": "esbuild index.tsx --bundle --outfile=index.js --format=esm --jsx=automatic --minify"
  },
  "dependencies": {
    "@google/genai": "^0.11.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-draggable": "^4.4.6",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.23.0"
  }
}